/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
}
module.exports = {
  output: 'export',
  reactStrictMode: true,
  typescript: {
    ignoreBuildErrors: true,
  },  
};
